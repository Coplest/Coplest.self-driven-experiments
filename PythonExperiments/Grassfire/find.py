def paths(world, max_distance, world_start_x, world_start_y):
    distance = max_distance
    
    likelyRoutes = []
    while True
        
        if(world[world_start_x + 1 ][world_start_y] < distance)
